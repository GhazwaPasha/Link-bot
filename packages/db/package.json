{
  "name": "@discord-forms/db",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "deploy": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "build": "pnpm --filter @discord-forms/shared run build && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@discord-forms/shared": "workspace:*",
    "@paralleldrive/cuid2": "^2.2.2",
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
